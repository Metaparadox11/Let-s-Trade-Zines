<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oliviamontoya.letstradezines">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUp" />
        <activity android:name=".Profile" />
        <activity android:name=".UploadProfilePic" />
        <activity android:name=".Search" />
        <activity android:name=".ViewProfile" />
        <activity android:name=".LoadMessages" />
        <activity android:name=".Messages" />
        <activity android:name=".AddZine" />
        <activity android:name=".RequestTrade" />
        <activity android:name=".Trades" />
        <activity android:name=".FAQ"></activity>
    </application>

</manifest>